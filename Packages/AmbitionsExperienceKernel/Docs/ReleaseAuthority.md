# Release Authority

A package-level Green result is not app release approval. App release approval requires package lint, repo truth audit, Xcode build, Xcode tests, screenshot matrix evidence, accessibility review, performance review, and rollback documentation.

`AmbitionsReleaseAuthority` models that evidence explicitly so Codex cannot confuse package validity with app readiness.
