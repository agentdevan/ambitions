# Semantic Causality

Living visuals must have a cause.

Allowed causes are capacity change, protected time approaching, closure residue change, source freshness change, proof attachment, goal pull change, recovery need change, privacy boundary change, or user inspection.

The reducer emits `AmbitionsVisualEffect` values only from runtime deltas. This prevents decorative movement and keeps visual evolution tied to the user's real day.
