# Repo Integration

Install into `Packages/AmbitionsExperienceKernel`.

Then wire the app target to import `AmbitionsExperienceKernel` and migrate top-level surfaces in this order:

1. Today / Reality Meridian
2. Time / LifeShape Field
3. Capture / Atmosphere Composer
4. Goals / Constellation Atlas
5. You / User System Profile

Migration rule:
Use the real repo as truth. Delete superseded generated visual packages only after the app builds and screenshots are captured.
