You are implementing AmbitionsExperienceKernel inside `agentdevan/ambitions`.

Goal:
Convert the package into the canonical experience layer for the native iPhone app. Do not treat it as a visual sample. Wire it to the real app surfaces, runtime state, tests, previews, and release gates.

Required order:
Run the 16 batches in `Codex/Batches` sequentially. Do not skip repo truth audit. Do not implement visuals that are not caused by semantic runtime state.

Hard product contracts:
- Today primary object: Reality Meridian.
- Start here: decision layer attached to Reality Meridian.
- Goals primary object: Constellation Atlas.
- Capture primary object: Atmosphere Composer.
- Time primary object: LifeShape Field.
- You primary object: User System Profile.

Validation:
Run package lint, repo truth audit, Xcode build, tests, preview/screenshot matrix, and readiness report. Report Green Yellow Red with exact blockers.

Rollback:
Every batch must be reversible through one commit revert. Do not mix unrelated migrations.
