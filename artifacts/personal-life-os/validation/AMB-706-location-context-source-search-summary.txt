AMB-706 / PLOS-084 Location context adapter source-search summary
Date: 2026-06-13 America/New_York

Commands:

- `rg -n "Location|CoreLocation|CLLocation|CLLocationManager|CLAuthorizationStatus|location|place|travel|commute|distance|timezone|cityRegion|precisePermissioned" Native/Ambitions Native/AmbitionsTests Sources AppUI docs/codex artifacts/personal-life-os/reports artifacts/personal-life-os/native-context --glob '!**/*.xcresult/**' --glob '!**/DerivedData/**' --glob '!**/.build/**'`
- `rg -n "Location|context adapter|permission" . --glob '!artifacts/**/script-output/**' --glob '!artifacts/personal-life-os/validation/**' --glob '!**/*.xcresult/**' --glob '!**/DerivedData/**' --glob '!**/.build/**' --glob '!build/**' --glob '!Library/**'`
- `rg -n "CoreLocation|CLLocation|CLLocationManager|CLAuthorizationStatus|import CoreLocation|NSLocation" Native/Ambitions Native/AmbitionsTests Sources AppUI project.yml Package.swift docs/truth artifacts/personal-life-os/native-context artifacts/personal-life-os/reports --glob '!**/*.xcresult/**' --glob '!**/DerivedData/**' --glob '!**/.build/**'`

Raw log sizes:

- `artifacts/personal-life-os/validation/AMB-706-location-context-source-search-log.txt` - 1,940 lines / 313,960 bytes
- `artifacts/personal-life-os/validation/AMB-706-required-location-context-search-log.txt` - 818 lines / 172,515 bytes

Findings:

- No active app source, test source, project manifest, package manifest, truth file, or native-context artifact imports `CoreLocation`.
- No active app source or test source contains `CLLocationManager`, `CLLocation`, `CLAuthorizationStatus`, or `NSLocation*` permission strings.
- No production `LocationContextAdapter` source type exists.
- `NATIVE_CONTEXT_MESH_ADAPTER_CONTRACT.md` names Location only as a future native-context source and explicitly excludes CoreLocation integration, runtime adapter implementation, permission prompting, entitlement changes, privacy manifest changes, release, accessibility, device, performance, and App Review proof.
- Existing source has local/manual Location ownership anchors in `LifeContextModels.swift`, including timezone, general location label, `LifeContextLocationPrecision`, travel radius, transportation access, location-dependent pathway flags, travel requirements, travel model projection, and `locationHistory` historical fact category.
- Existing runtime/projector source uses location/travel context as local fit evidence through `StepCandidateFieldGenerator.swift`, `PersonalizationFactorLedgerBuilder.swift`, `PrivateLifeRuntimeKernelContracts.swift`, and You inspection rows in `YouFeatureService.swift`.
- Existing tests include fixture coverage for city/region, user-entered place, timezone-only, and precise-permissioned labels in profile fixtures; those fixtures are not CoreLocation integration proof or production runtime permission proof.

Conclusion:

AMB-706 should remain a documentation/control-plane downstream contract. The safe launch baseline is manual/coarse local location and travel context with no platform Location permission ask. Any future useful path must be separately scoped and limited to coarse, local, revocable, never-transmitted Location summaries after strong value proof.
