# B02 Phase 0 specification review

Initial result: `FAIL` with six blockers.

## Resolutions

1. Replaced phase-only planning with twelve task-sized subagent assignments,
   exact path registry, interfaces, RED/GREEN assertions and commands, render
   checkpoints, reports, reviews, and commit scopes.
2. Limited Phase 1 tests to grammar; moved root and focus behavior to their
   owning phases.
3. Selected exact protected/fixed/external/open node, Start Here relief, and
   Dock Peek geometry.
4. Added explicit immutable timeline-role and context-seam snapshots, exact
   quiet/dense/very-dense selection, source-support matrix, and absent controls.
5. Removed B01 evidence from B02 authorized paths.
6. Added named no-color/long-localization frames, all evidence destinations,
   and the missing four motion/material warm runs with required fields.

Re-review: repaired through successive gates; final status below.

First re-review found two remaining blockers. Full Day now records an explicit
initial/returned origin, inserts the existing primary Step as its source-backed
Now row, permits Step depth only from initial Today, rejects all post-return
Step opens, and restores exact phase/focus on both Back boundaries. Localized
copy now includes all six command titles and the Time owner label with an Arabic
no-English guard.

Final fresh review: `PASS`.

The final contract resolves Now by route origin: initial Today maps to the
nursery Step and returned Today maps to `step.send-launch-brief`. The same
resolver drives Scroll to Now, while the nursery Step remains settled,
subordinate, and read-only after return. Full Day remains a read-only Today
projection and does not expand Time ownership.
