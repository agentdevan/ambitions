# Visual Owner Workshop Report

Status: `AWAITING_EXPLICIT_OWNER_DIRECTION_SELECTION`

Artifact state: `EXPLORATION`

Repair commit: this report is contained in the single bounded repair commit; resolve the exact immutable SHA with `git rev-parse HEAD` after commit creation.
Frozen repository base: `dcdd0e0e3e91a19e6988c7c372fea5cb1fca6847`

## Outcome

The existing isolated workshop page was repaired in place without deleting or modifying legacy or current-candidate pages:

- Page: `69:2` — `EXPLORATION — Owner Visual Workshop — 2026-07-14`
- Direction A section: `69:3`
- Direction B section: `69:4`
- Direction C section: `69:5`
- File key: `Oik7612LSTUHWsNRFoTlTJ`

Each direction still contains exactly 11 standard 393×852 product viewports and one populated 393×852 accessibility-size Today viewport. All 36 outer viewport IDs were preserved.

- A is now one integrated quiet spatial system across all 12 frames: object-specific fields, orbits, living paths, and lifecycle traces with minimal containers.
- B is now one materially different edge-to-edge editorial/native system across all 12 frames: asymmetric gutters, numbered sections, large typography, and rules without A-style cards.
- C now carries meridians, traces, orbits, capacity bands, and opaque fallbacks through all 12 frames instead of only the four anchors.
- The visual Goal Path remains horizontal. Accessibility uses an ordered semantic-list counterpart and never calls it a vertical Goal Path.
- Each accessibility Today frame has materially larger type, real vertical reflow, unchanged information/order, no horizontal scroll, and non-color square/circle/triangle state shapes.
- Every represented interaction has a named envelope of at least 44×44 pt. Glyphs may be smaller. This remains visual design input, not runtime hit-testing or accessibility proof.

Every product viewport carries accurate shared `EXPLORATION` data in the stable `ambitions.visual_owner_workshop` namespace for frozen SHAs, blueprint/stable requirement IDs, composition system, accessibility posture, represented target count, touch-target contract, and proof ceiling.

## Direction and frame IDs

### A — Quiet Spatial Stage

`71:4`, `71:50`, `71:99`, `73:2`, `73:59`, `73:109`, `74:2`, `74:33`, `74:67`, `75:2`, `75:27`, `75:72`

Linked non-viewport Trash companion: `78:13`

### B — Editorial Native

`80:4`, `80:51`, `80:101`, `80:152`, `81:2`, `81:53`, `81:131`, `81:163`, `82:2`, `82:24`, `82:49`, `82:95`

Linked non-viewport Trash companion: `82:129`

### C — Atmospheric Temporal Field

`83:4`, `83:52`, `83:103`, `83:155`, `84:2`, `84:54`, `84:133`, `84:166`, `85:2`, `85:25`, `85:49`, `85:96`

Linked non-viewport Trash companion: `85:129`
Anchor semantic annotations: `88:2`, `88:3`

## Material Figma calls and results

1. Read-only live inspection compared the current candidate and A/B/C at presentation scale. It confirmed that A/B shared a card skeleton, C lost its field grammar on secondary screens, and the accessibility/touch-target representation was insufficient.
2. Code Connect remained seat-gated; no Code Connect mapping was written. Local SwiftUI and Figma evidence was used only for feasibility review.
3. The first consolidated A repair call failed atomically because linked companion `78:13` is a `GROUP`, not a `FRAME`; Figma debug UUID `021e32bd-e689-4e5e-af44-b3d18d84a0ca`. Read-back confirmed no mutation persisted. The corrected call preserved the companion and all outer IDs.
4. A, B, and C were then rebuilt in place across all 36 viewports with direction-specific composition systems and explicit named interaction envelopes.
5. C annotations `88:2` and `88:3` were updated in place to preserve the canonical horizontal visual Goal Path and name only an ordered semantic-list accessibility counterpart.
6. Direct screenshot inspection found representative copy overlaps; measured repairs were applied without changing frame concepts or IDs.
7. Precise Figma `absoluteRenderBounds` validation returned zero text clipping and zero text overlap across all 36 frames.
8. The presentation-scale montage found four final polish classes: A Goals word integrity, A Offline state-label integrity, Month control integrity across all three Time frames, and C Capture rule placement. Those nodes were repaired in place; A Time and B Capture label fit was checked in the same bounded pass.
9. Final validation returned 36 frames at exactly 393×852, `approval_state=EXPLORATION`, non-empty mappings, exact represented target counts, and zero named target envelopes below 44×44 pt.
10. Only the 36 changed direction screenshots were regenerated. The 18 frozen current-candidate PNGs were not rewritten.
11. Page scope remained `69:2`; no legacy/candidate page or node was mutated, deleted, approved, or superseded.

## Durable files

- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/OWNER_REVIEW_PACKAGE.md`
- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/OWNER_REVIEW_PACKAGE.json`
- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/screens/current-candidate/` — 18 reused SHA-bound product-only proofs
- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/screens/direction-a-quiet-spatial-stage/` — 12 product-only proofs
- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/screens/direction-b-editorial-native/` — 12 product-only proofs
- `docs/qa/evidence/2026-07-14-canon-visual-owner-workshop/screens/direction-c-atmospheric-temporal-field/` — 12 product-only proofs
- `.superpowers/sdd/visual-owner-workshop-report.md`

The JSON is key-sorted, newline-terminated, and timestamp-free. It records every workshop node, direct link, screenshot path, SHA-256, direction tradeoff, owner decision, and the claim ceiling.

## Verification

- Figma structure: 3 sections × 12 product viewports; passed.
- Viewport dimensions: 36 × 393×852; passed.
- Final candidate/A/B/C presentation-scale montage: all 12 frames per direction inspected; A, B, and C remain materially different composition systems across the full set; passed.
- Precise rendered-text bounds: zero clipping and zero overlap across 36 frames; passed.
- Represented interaction envelopes: zero named targets below 44×44 pt; passed. Glyphs may be smaller; runtime hit testing remains outside the claim ceiling.
- Shared artifact status: `EXPLORATION`; passed.
- Shared metadata namespace: `ambitions.visual_owner_workshop`; mappings, frozen SHAs, composition, accessibility posture, target contract/count, and proof ceiling present; passed.
- Direct Figma links: node-specific for all 36 exploration frames and 18 frozen candidate proofs.
- Screenshot corpus: 54 PNG files; passed.
- Screenshot hashes: recorded in `OWNER_REVIEW_PACKAGE.json`.
- Current-candidate reuse: hashes match the frozen rebaseline registry.
- JSON parse/determinism: validated with `jq`.
- Product source changes: none.
- Figma legacy/candidate deletion: none.
- Final Architecture Tree inspected: not applicable; no source implementation or path mutation.
- Claim ceiling: visual design input only; no source, runtime, rendered-app, accessibility, device, privacy/legal, distribution, approval, or release proof.

## Residual Minor

- This report cannot embed its own immutable repair commit SHA without changing that SHA. Resolve the package commit with `git rev-parse HEAD` after commit creation. No visual, mapping, validation, approval, or release claim depends on this self-reference.

## Recommendation and concern

Recommendation: Direction A — Quiet Spatial Stage.

Remaining concern: explicit owner selection is still absent. Direction C also requires an explicit dark/material-intensity decision if selected. Code Connect remains seat-gated. These are decision/tooling boundaries, not missing workshop frames.

No direction is approved, authoritative, superseding, active, or implementation-ready.
