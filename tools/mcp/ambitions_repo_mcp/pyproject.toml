[project]
name = "ambitions-repo-mcp"
version = "0.1.0"
description = "Read-only local MCP server for Ambitions repo truth, EFC gating, and batch safety."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ambitions-repo-mcp = "server:main"

[tool.pytest.ini_options]
pythonpath = ["."]
