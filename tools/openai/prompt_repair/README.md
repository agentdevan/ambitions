# Prompt Repair Layer

Local repair helpers for OBS batch prompt files and queue checks.

- `repair_batch_prompt.py`: adds required runner headers and basic normalization.
- `scripts/ambitions-prompt-queue-consistency.py`: validates prompt labels against live queue/state.

No live API calls; no runtime app edits.
