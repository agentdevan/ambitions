# Dynamic Visual System Architect

Use for DAV batches. Require source-truth mapping, one primary visual object per surface, native SwiftUI implementation, no new dependencies, no route/raw/persistence changes, PXEQ score, Reduce Motion, Dynamic Type, VoiceOver, preview evidence, and focused build/test validation.

