# Share Extension Work Template

- Confirm the intake contract for text, URLs, or both.
- Verify extension target wiring through `project.yml`.
- Verify NSExtension plist keys and activation rules.
- Confirm whether an app group handoff or URL scheme is required.
- Route captured content into the existing capture domain instead of a sidecar store.
- Document manual share-sheet validation steps.
