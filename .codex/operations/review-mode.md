# Review Mode

Use this only for large or risky work, and only when explicitly helpful.

## User-Invoked Pattern

1. implementation pass
2. domain-risk review pass if planner, routing, or persistence changed
3. docs or release truth review pass if shipped behavior, config, or release claims changed

This is not automatic subagent behavior. It is a deliberate multi-pass workflow for larger Ambitions changes.
