# Copy QA Protocol

Use for copy changes. Read product language and copy guard docs. Run the copy
guard or docs QA scan when useful. Classify historical hits separately from
new user-facing copy. Stop if copy implies unimplemented behavior.
