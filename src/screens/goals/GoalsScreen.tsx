import { PlaceholderScreen } from "../../components/ui/PlaceholderScreen";

export function GoalsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Goals"
      title="Structured ambition lives here"
      body="This area is reserved for the long-range goal hierarchy, domain framing, and decomposition entry points."
    />
  );
}
