import { PlaceholderScreen } from "../../components/ui/PlaceholderScreen";

export function InsightsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Insights"
      title="Measured reflection, not vanity charts"
      body="Insights will eventually expose completion patterns, friction signals, and strategy adjustments in a restrained, trustworthy way."
    />
  );
}
