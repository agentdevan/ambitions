# Signature Object Moat Gap Map — MOAT-ALIGNMENT-01

This map tracks moat alignment gaps by surface and next implementation sequence.

## Today / Reality Meridian

- Gaps:
  - Runtime implementation still not wired to `AmbitionGraph`-based recommendation payloads.
  - Closure-to-proof transitions still rely on legacy screen behavior for many flows.
- Compatibility seams:
  - Legacy compatibility symbols remain in historical runtime references.
- Next sequence:
  - Connect recommendation sources to `RecommendationTrace`,
  - enforce "Why this?" with source/explainability controls,
  - expose proof continuity in closure paths.

## Goals / Constellation Atlas

- Gaps:
  - No durable persistence migration yet from legacy goal/thread-first storage assumptions.
  - Proof trail and reflection surfaces still not consistently sourced from `AmbitionGraphModels`.
- Compatibility seams:
  - Older names remain in legacy flow names and historical views.
- Next sequence:
  - Add ambiguity-safe mapping between legacy Goal thread data and new `AmbitionGraphSnapshot`,
  - add proof continuity fixtures for `Move/Still Counts/Needs Recovery`.

## Capture / Atmosphere Composer

- Gaps:
  - UI routing exists, but model-level proof/constraint destination is not yet fully tested across all capture surfaces.
- Compatibility seams:
  - Existing capture route names and legacy flow names are retained for backward compatibility.
- Next sequence:
  - Expand runtime path for `proofItem` and `constraintItem`,
  - add non-shaming closure handling for capture-driven holds.

## Time / LifeShape Field

- Gaps:
  - Time fit/opportunity and proof-opportunity are still partly conceptual in this phase.
  - No deterministic service contract yet ties `RecoveryThread` state to Time reflow controls.
- Compatibility seams:
  - `Plan`/`.plan` remains an internal compatibility path.
- Next sequence:
  - Add source-driven proof-opportunity mapping for shape actions,
  - tighten closure + recovery controls around small continuations.

## You / User System Profile

- Gaps:
  - Local trust controls remain conceptual and are not yet surfaced through runtime control seams.
- Compatibility seams:
  - legacy profile naming remains in historical source paths.
- Next sequence:
  - Add User System Profile / Personal Runtime control surfaces over learning and recommendation controls,
  - add explicit inspect/delete/reset affordances with receipts.

## Current Compatibility Seams

- `Plan`, `PlanScreen`, `Profile`, `Captures` are compatibility terms retained as internal references.
- This phase treats those terms as migration names only, not active user-facing truth.

## Next Implementation Sequence

1. Close the runtime gap for `RecommendationTrace` explainability on Today/Start Here.
2. Add proof continuity and reflection paths in Goals and Time services.
3. Add capture routing for proof/constraint to explicit persistence adapters.
4. Add Personal Runtime control surfaces for learning, trust, and reset/delete/read actions.
