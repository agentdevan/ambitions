# Owner Review

Status: `SELECTED_FOR_RENDERING`

The Goals R01 native implementation is preserved as a rejected structural spike:

`REJECTED_STRUCTURAL_SPIKE — PRESERVE_SEMANTICS / REPLACE_VISUAL_MODEL`

The owner selected the following text directions for first-round standalone Goals-root rendering:

- `AVF-GOALS-S08-INT-D02-R00 — Open Horizon Passage`
- `AVF-GOALS-S08-INT-D04-R00 — Pursuit Aperture`
- `AVF-GOALS-S08-INT-D06-R00 — Living Fold`

The owner held D01, D03, and D05 from this round. This decision does not select a final Goals direction, authorize native prototyping or SwiftUI implementation, authorize runtime integration, or set `APPROVED FOR SWIFTUI` to true.

The authorized rendering scope is exactly one standalone full-screen Goals root for each selected direction, using identical fixture, device, appearance, and density. Focused Goal, Goal Path, consequential relationship, and Accessibility Dynamic Type renderings remain gated on a later owner finalist selection.

## Decision boundary

This package does not authorize:

- editing the rejected SwiftUI views;
- changing Goals fixtures or typed routes;
- generating direction images beyond the three authorized root renderings;
- native prototypes;
- production Goals implementation;
- runtime integration or mutation;
- broad frontend reconstruction;
- component APIs or final tokens;
- Figma or Code Connect authority;
- production screenshot baselines; or
- `APPROVED FOR SWIFTUI`.

## Next decision

- select one or two roots for finalist depth rendering;
- request a research revision;
- request replacement directions;
- resume the rejected R01 structure; or
- pause Goals calibration.
