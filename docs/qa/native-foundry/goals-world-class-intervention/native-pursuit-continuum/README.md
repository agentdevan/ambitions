# Native Pursuit Continuum rendering gate

Status: `READY_FOR_OWNER_NATIVE_PURSUIT_CONTINUUM_REVIEW`

Direction: `AVF-GOALS-S08-INT-D09-R00 — Native Pursuit Continuum`

Owner disposition executed: `REQUEST_NEW_GOALS_STRUCTURAL_BRANCH`

This package contains exactly three standalone full-screen visual evaluation references. D09 preserves D08's corrected canonical IA while replacing its directory, list, and document anatomy with one persistent Goal object at three resolutions.

1. [`AVF-GOALS-S08-INT-D09-R00-goals-root.png`](AVF-GOALS-S08-INT-D09-R00-goals-root.png) — Goals root, showing Life Area fields with one pursuit signal each.
2. [`AVF-GOALS-S08-INT-D09-R00-home-life-area.png`](AVF-GOALS-S08-INT-D09-R00-home-life-area.png) — Home, showing three compact pursuit objects with supported, forming, and open postures.
3. [`AVF-GOALS-S08-INT-D09-R00-focused-goal.png`](AVF-GOALS-S08-INT-D09-R00-focused-goal.png) — `Welcome our baby home`, expanding the same Goal anchor into accepted truth, Proof/history provenance, current movement and Goal Path, open possibility, and protected relationship depth.

## Controlled conditions

- source repository SHA: `75ec538bc33a6c531cfbae59938d3be319267f43`
- synthetic fixture family: `GoalsNativeCalibrationFixture.preparingForBaby`
- viewport: iPhone 17 Pro portrait equivalent, `853 × 1844`
- appearance: Dark
- content size: standard Dynamic Type
- locale: English
- `production_baseline = false`

All frames use one matte graphite content plane, system typography, compact right-edge Goals dock Peek, and native route ownership. Root retains its root crown; Home and Goal detail use native Back ownership.

## Persistent Goal anatomy

The provisional canonical anchor is a small open bracket paired with a short accepted-truth seam. A single notch identifies the active present. Support ticks attach directly to the seam, movement begins from a repeated anchor at the forward resolution, and open possibility remains visibly unfinished.

The anchor is intentionally not a category icon, node bullet, progress mark, timeline, chart, stepper, card silhouette, or final reusable component API. Exact geometry remains subject to owner review.

## Goals root

Home, Relationships, and Career remain comparable Life Area drilldown targets while showing different semantic postures:

- Home exposes a supported Goal and one current movement.
- Relationships exposes a protected consequence through an open boundary.
- Career exposes a contained accepted state and active thread.

The frame has no bullet summaries, repeated purple paragraphs, inline Goal expansion, Linked Goal Lens, tabs, metrics, or Goal-level navigation.

## Home Life Area

Three Goal objects reuse the same anchor but vary by fixture truth:

- `Welcome our baby home` has an accepted-truth seam, three attached moments, and the active nursery movement.
- `Make the house easier to run` shows a quietly forming truth and contained evening-reset thread.
- `Finish essential move-in work` retains an unfinished anchor and open sequence without inventing a next command.

There are no arbitrary Goal icons, repeated `Next` rows, inline Goal expansion, or Linked Goal Lens at this depth.

## Focused Goal

The compact Goal identity no longer consumes the viewport. Its pursuit continuum gives each native depth a semantic origin:

- `3 recorded moments` and `Proof & history` disclose directly from the accepted-truth seam.
- `Paint the nursery wall` and `Open Goal Path` disclose from the movement field.
- `Beyond this remains open` occupies the unresolved forward field.
- `Protect our first weeks together` opens from a labelled protected boundary that modifies the same pursuit.

No Path/Proof/History menu is appended at the bottom, and no graph connector, chart, timeline, roadmap, stepper, percentage, or giant title is used.

## Prompt record

Root prompt: preserve shell and IA, replace D08's repeated directory template with three Life Area fields, and introduce one open-bracket Goal anchor with supported, protected, and contained semantic variants. Keep one Life Area NavigationLink disclosure per field and remove inner Goal navigation.

Home prompt: preserve native Back and shell, replace arbitrary Goal icons and repeated rows with three compact variants of the same anchor, truth seam, support, movement, and open posture. Keep all Goals non-expanded.

Focused-Goal prompt: replace D08's hero document with one pursuit continuum. Attach Proof/history to truth, Goal Path to movement, future openness to the unfinished field, and relationship depth to a protected boundary. Remove graph-like connectors during refinement.

Built-in image generation and edit tools produced the final assets. Card-like, graph-like, over-purple, and density-defective intermediate generations were rejected and were not copied into the repository.

## Proof ceiling

These are generated visual evaluation references. They are not native SwiftUI screenshots, NavigationStack or interactive-Back proof, focus-restoration proof, Dynamic Type or VoiceOver proof, interaction proof, device proof, shell validation, runtime proof, production screenshot baselines, or final Goals-direction acceptance. SwiftUI implementation remains unauthorized and `APPROVED FOR SWIFTUI` remains false.
