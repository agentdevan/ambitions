# Native Pursuit Continuum, Materialized rendering gate

Status: `READY_FOR_OWNER_NATIVE_PURSUIT_CONTINUUM_MATERIALIZED_REVIEW`

Direction: `AVF-GOALS-S08-INT-D09-R01 — Native Pursuit Continuum, Materialized`

Owner disposition executed: `SELECT_D09_FOR_SYNTHESIS / REQUEST_NARROW_D09_R01_REVISION`

This package contains exactly three standalone full-screen generated visual evaluation references. D09-R01 retains D09's persistent cross-depth Goal anatomy while replacing its literal bracket and annotation geometry with open matte material shelves, embedded Proof, explicit native operating affordances, qualitative future certainty, and relationship-driven boundary change.

1. [`AVF-GOALS-S08-INT-D09-R01-goals-root.png`](AVF-GOALS-S08-INT-D09-R01-goals-root.png) — Goals root, with construction, protection, and containment postures across Home, Relationships, and Career.
2. [`AVF-GOALS-S08-INT-D09-R01-home-life-area.png`](AVF-GOALS-S08-INT-D09-R01-home-life-area.png) — Home, with the selected Goal carried by a supported truth shelf and embedded Proof provenance beside two quieter peer pursuits.
3. [`AVF-GOALS-S08-INT-D09-R01-focused-goal.png`](AVF-GOALS-S08-INT-D09-R01-focused-goal.png) — `Welcome our baby home`, with accepted truth, attached Proof, current Goal Path action, possible and conditional future, and a protected relationship boundary.

## Controlled conditions

- source repository SHA: `51ad2b9fefcdeea9cf5707a7d825fb34aa595305`
- synthetic fixture family: `GoalsNativeCalibrationFixture.preparingForBaby`
- viewport: iPhone 17 Pro portrait equivalent, `853 × 1844`
- appearance: Dark
- content size: standard Dynamic Type
- locale: English
- `production_baseline = false`

All frames use one matte graphite content plane, system typography, compact right-edge Goals dock Peek, and native route ownership. Root retains its root crown; Home and Goal detail use native Back ownership.

## Materialized Goal anatomy

The provisional Goal anchor is a tiny faceted material origin paired with a shallow, open-ended truth shelf. The shelf carries accepted truth and embeds its Proof provenance at the foundation. Current movement attaches through alignment and one native action. Possible and conditional future postures lose material definition without implying chronology, distance, or percentage. A protected relationship changes the lower boundary of the same pursuit field.

The anatomy is intentionally not a bracket, technical annotation, card silhouette, progress indicator, chart, timeline, roadmap, stepper, graph, or final reusable SwiftUI API. Exact geometry remains subject to owner review.

## Goals root

The root remains the native Life Area index. Its fields share identity-first hierarchy but vary according to semantic posture:

- Home exposes active construction through supported truth, embedded provenance, and current movement.
- Relationships exposes protection through a warm mineral boundary change.
- Career exposes containment through a compact darker accepted-state shelf.

The frame contains no bullet summaries, inline Goal expansion, Goal-level route, category-color system, metric, dashboard, or repeated identical Life Area template.

## Home Life Area

The selected `Welcome our baby home` pursuit retains the same material origin, accepted truth, embedded provenance, current movement, and explicit `Open Goal` action. Peer Goals remain native drilldown targets but use quieter forming and contained postures. No Goal expands inline or implies ranking.

## Focused Goal

The accepted-truth shelf is the primary state boundary. `Corner clear`, `Color confirmed`, and `Wall primed` are its attached provenance and expose `Inspect Proof`. `Paint the nursery wall` owns the explicit `Open Goal Path` action. `Assemble the crib` is possible later; `Arrange final furniture after delivery` is conditional and intentionally less bounded. `Protect our first weeks together` changes the pursuit's lower boundary and remains a native relationship disclosure.

## Accessibility transformation concept

No accessibility frame was authorized. The proposed semantic equivalent at Accessibility Dynamic Type is a vertical, naturally scrolling order: Goal identity and owner; accepted truth; Proof disclosure; current movement and Goal Path action; possible later; conditional future; protected relationship. The Goal object therefore does not depend on thin line intersections, fixed horizontal placement, color, transparency, or motion.

## Prompt record

Built-in image generation and editing produced the final assets from the preserved D09-R00 frames.

- Root prompt set: retain the canonical Life Area index and shell; replace literal brackets with shallow open truth shelves; vary Home, Relationships, and Career by construction, protection, and containment; embed named Proof provenance; remove metric counts, L-shaped protection geometry, and outlined containment.
- Home prompt set: retain native Back, three stable Home Goals, and shell; carry the selected Goal through an open accepted-truth shelf, named Proof provenance, current movement, and `Open Goal`; remove the generated enclosing card and peer progress-like bars.
- Focused prompt set: retain native Back and fixture truth; materialize accepted truth and Proof; attach `Open Goal Path`; express current, possible, and conditional certainty through declining boundary definition; let the protected relationship alter the pursuit boundary; remove the generated rounded possible-later panel.

Intermediate card-like, metric-like, bracket-like, and progress-like generations were rejected and were not copied into the repository. The final prompts were executed with the built-in image-generation tool; no fallback CLI was used.

## Proof ceiling

These are generated visual evaluation references. They are not native SwiftUI screenshots, NavigationStack or interactive-Back proof, focus-restoration proof, Dynamic Type or VoiceOver proof, interaction proof, device proof, shell validation, runtime proof, production screenshot baselines, or final Goals-direction acceptance. SwiftUI implementation remains unauthorized and `APPROVED FOR SWIFTUI` remains false.
