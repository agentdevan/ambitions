# Reviewer findings

## Phase 0

The first specification and quality reviews each returned six blockers. The
issues were planning precision, Phase ownership, unresolved geometry, explicit
stress-state/copy contracts, Full Day Back semantics, accessibility proof
classification, immutable reference durability, and complete media/benchmark
inputs. All were repaired before implementation. Re-review status is pending.

The first re-review found incomplete localized navigation/Time-owner fields,
unsafe post-return Full Day Step depth, no stable Now anchor, and shallow media
metadata validation. Repairs added the complete copy map, origin-bearing Full
Day routes with post-return read-only behavior, stable Now/focus identities, and
exact per-artifact validation commands. A second fresh re-review is required.

The quality final review additionally required an exhaustive inherited product/
accessibility-copy inventory and exact render-source binding. Those contracts
now forbid literal product/AX strings in journey views, scan the complete Arabic
label tree, enforce unique artifact paths and the approved fixture family, and
prove that evidence-only commits do not change Swift source after capture.

A later quality gate found masked-failure risk in metadata shell loops. They now
use strict shell mode and explicit failure exits for every artifact property. A
later specification gate found a stale post-settlement Now identity. The
contract now resolves Now from Full Day origin and requires the returned Start
Here to own Now while retaining the nursery Step as settled/read-only context.

The next gate caught the superseded fixed-Now wording in the evidence fixture
contract and an ambiguous phase/task commit split. The fixture record now uses
the origin-specific resolver. Task 01 explicitly includes the anatomy refactor;
Phase 2 explicitly integrates the two separate shell and root commits.

The final boundary gate found unresolved shorthand for motion, adaptivity, and
capture ownership. Tasks 09 and 10 now enumerate exact paths, Task 12 is
evidence-only, and Phase 8 explicitly integrates—rather than replaces—the
three ordered task commits.

The next gate found Task 11 could edit unspecified source and metadata paths
could escape the evidence root. Task 11 now owns three exact test files and
routes source defects back to implementation. The final validator uses a full
ID/path allowlist and rejects absolute or escaping paths.

Final Phase 0 specification review: `PASS`.

Final Phase 0 quality review: `PASS`. Tasks 01–12 have exact ordered ownership;
Task 11 is test-only; Task 12 is evidence-only; and the 26/5/4 media inventories
are bound to exact contained paths, hashes, measurements, fixture identity, and
one immutable render-source SHA.

Later phase findings and their dispositions will be appended without rewriting
this history.

## Phase 1 — provisional visual grammar

The first specification and quality reviews blocked handoff because only the
Light specimen had native inspection and the Differentiate Without Color
preview did not activate the actual SwiftUI accessibility environment. The
repair rendered Light, Dark, Increased Contrast, Differentiate Without Color,
and Accessibility Dynamic Type and bound the no-color specimen to the real
environment value.

Final Phase 1 specification review: `PASS`.

Final Phase 1 quality review: `PASS`. The review retained all nine non-color
node shapes and confirmed that the grammar remained journey-local, package-
backed, matte, semantic-type driven, and free of dependency or runtime drift.

## Root, Full Day, focus, review, settlement, return, and recovery

Native iteration and focused reviews found and repaired concrete defects:

- Compact root relationships and headings were allowed to wrap rather than
  collapsing into narrow columns.
- The root overview became deterministically bounded and never invents an open
  lane.
- Full Day now records initial/returned origin, restores the truthful phase and
  focus, and makes returned-origin objects read-only.
- Accessibility 5 focused depth moved its action into natural scrolling and
  restored the action's own accessibility identity.
- Parent-pursuit anatomy recomposes vertically under accessibility pressure.
- Review, saving, settlement, return, and recovery retained the existing
  semantic transitions while adopting the B02 continuity grammar.
- Returned Today preserves the settled nursery Step and shows the revealed
  Start Here exactly once.

No review finding authorized a live runtime, app-entry, IA, fixture-truth, or
dependency change.

## Motion, performance, accessibility, and adaptivity

Motion/performance review removed ad hoc animation durations, redundant
saturated crown callbacks, and repeated timeline selection. The common policy
uses a short state-bound animation and removes spatial animation under Reduce
Motion. ETTrace and memgraph were not run because no reproducible performance or
retention symptom warranted them.

Accessibility/adaptivity review found and repaired a 26-point disclosure target,
hidden continuation indicators, a color-sensitive comparison seam, and an
under-filled Pro Max root. The final repair retained native scroll indicators
with a dock-clear margin, 44-point disclosure targets, structural
current/proposed distinctions, and viewport-aware root rhythm without fixed
screenshot coordinates. Earlier Arabic/RTL diagnostic work remains historical
and is excluded from the final English-only proof package.

Final Task 10 specification review: `PASS`.

Final Task 10 quality review: `PASS`; no remaining Simulator-testable blocker
was reported.

## Task 11 — regression and interaction guards

Commit: `133c040e5` (`test(foundry): lock open continuity behavior`).

Task 11 added package boundary/system-font guards, Reduce Motion review
semantics, deterministic TFCS-J01 through J05 English-only recording drivers,
and an intentional native-scroll attachment. A real rendered defect was found:
the two recovery actions were approximately 42.25 points high. The bounded
repair gives the existing native-sheet labels a 48-point intrinsic minimum,
retaining the exact 44-point rendered threshold.

Observed Task 11 verification: 50 package tests passed; five recording drivers
passed; focused recovery target tests passed; the history disclosure journey
passed; and `git diff --check` was clean before commit.

Task 11 specification reviewer verdict: `PASS`.

Task 11 quality reviewer verdict: `PASS`.

Both reviewers confirmed that the recovery-label repair was narrowly scoped,
the regression guards preserved the fixture/runtime boundary, and the final
capture must use English-only drivers. Historical RTL-only cases remain
excluded from capture, review, and proof claims.

## Final Phase 9 gate

Status: `PASS`.

The fresh specification/accessibility/authority reviewer found no remaining
scope, authority, metadata, accessibility, or validation blocker. The review
confirmed 25 English-only screenshots, five Foundry-host recordings, four
contact sheets, `production_baseline = false`, unchanged authority, no product
app/runtime integration, and honest direct-device and localization ceilings.

The fresh visual reviewer scored the final matrix 66 of 75, an average of 4.40.
Every category scored at least 4. The reviewer found one concrete evidence
defect: TFCS-J03, J04, and J05 retained a post-test Simulator Home transition at
the tail even though metadata said it was removed. Those three recordings were
retrimmed to 44.5, 23.7, and 13.898 seconds, respectively. Their hashes and
metadata were regenerated, and fresh 99-percent-duration samples show the
intended Foundry-host journey state rather than Home. J01 and J02 were unchanged.

Final specification/accessibility/authority verdict: `PASS`.

Final visual-quality verdict after bounded recording repair: `PASS`.

Owner acceptance remains a separate decision. Final screenshots, recordings,
contact sheets, and review media are English only. RTL and localization proof
remain open.
