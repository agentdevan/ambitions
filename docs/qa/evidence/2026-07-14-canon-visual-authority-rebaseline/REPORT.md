# Visual Authority Rebaseline Phase 3/4 Candidate Report

Status: frozen candidate pending owner workshop, explicit approval, and exact-range independent review
Authority state: shadow and non-authoritative
Gate B: pending; task-pack visual selection is blocked and no authority transition is approved
Repository base: `4d73857b1c7ec86a0ce4293219c49d56bec18864`

## Bounded result

The selected C4 Quiet Field direction is represented by one offline, deterministic visual-authority candidate registry for the additive Figma file `Oik7612LSTUHWsNRFoTlTJ`. The registry binds `147` authority records, including `18` compact masters proposed for conditional selection only after a future approved Gate B and `47` generic skeleton screens retained only as failure evidence, to canon revision `1`, source SHA `ffd462ab52c0eff798071333388a051d9f3e55f3`, and content SHA `0ac3656f1f55c0514ada19da8b36b8a090628e4fa1648a6aaee3f660a3ed27bb`.

Coverage resolves `324/324` visual requirements and `433/433` inventoried visual states. Of those states, `263` are authority-eligible after Gate B, `4` are future-gated, and `166` remain gap-blocked. Broad screen or requirement scope that includes a future-gated or gap-blocked state fails closed; only an exact eligible state can resolve that mixed screen binding.

The replacement registry also records `18` canonical objects, `12` principal journeys, `11` cross-cutting standards, and `9` sensitive exposure channels. It validates every node's stable authority ID, binary `CANDIDATE` / `ARCHIVE` / `FAILURE_EVIDENCE` posture, Figma provenance, canon provenance, requirement IDs, accessibility variants, SwiftUI plausibility posture, implementation status, and proof ceiling.

The `18` candidate masters resolve all `47` screen mappings and all `433` state variants. Each candidate has one canonical product node and three distinct product-only, hash-bound proof artifacts: hero, viewport, and presentation (`54` artifacts total). Anatomy/specification boards are excluded from product proof. The presentation contract permits root navigation only for Today, Goals, Time, and You; Capture and Search have no root dock; Trust/Proof remains contextual inspection. All candidate masters carry the exact seven accessibility presentations: Standard, Large Text, Accessibility Size, Reduce Motion, Reduce Transparency, Increase Contrast, and VoiceOver Order.

## Non-destructive reconciliation

The legacy Figma file `SWtHm9ouHTPbEFfNrrtZwv` remains unchanged. Fifteen legacy nodes have proposed non-destructive dispositions and candidate replacement IDs. Nine repo-reference downgrades exist only as an unmerged candidate proposal; this report does not approve or apply legacy supersession or Figma metadata mutation. Existing active authority remains unchanged outside this candidate branch. The prior reconciliation receipt remains hash-bound at `a32410383e0536daaebf968b88de3f17809acc294e72084dbb55dc34968cbe96` so changing or removing that provenance fails closed.

No deletion, archive, consolidation, or destructive mutation is authorized. Gate C remains withheld.

The live replacement index remains rooted at `37:438`; the compact anatomy, accessibility-class, and product-only proof roots are `51:2`, `54:2`, and `62:62`. The prior generic screen matrices remain labeled failure evidence and are never task-pack eligible. No production Swift, runtime behavior, persistence, product law, authority cutover, AGENTS guidance, skill, or CI boundary changed in this slice.

## Deterministic enforcement

The compiler validates the tracked registry, current UX blueprint, requirement dispositions, state inventory, all `54` product-only proof digests, and the retained legacy receipt using only local checkout bytes. While Gate B is pending, old and new visual targets remain unable to authorize source work. After Gate B, only explicitly eligible candidate masters can resolve. A stale canon revision/content hash, corrupted evidence, future state, gap state, absent mapping, binary-label mismatch, failure-evidence selection, or over-broad mixed-state scope fails closed.

## TDD receipt

Focused RED for the consolidated review repair was observed with:

```text
/Users/devan/.local/share/uv/python/cpython-3.12-macos-x86_64-none/bin/python3.12 -m unittest tests.canon.test_visual_authority_rebaseline.VisualAuthorityRebaselineTests.test_binary_labels_and_failure_evidence_never_authorize tests.canon.test_visual_authority_rebaseline.VisualAuthorityRebaselineTests.test_candidate_shell_accessibility_and_proof_contract_is_exact
exit 1
Ran 2 tests; 2 failures. The old registry had no binary eligibility boundary and did not provide the compact candidate-master, exact shell/accessibility matrix, or product-only proof contract.
```

Focused GREEN after the bounded consolidated contract correction:

```text
/Users/devan/.local/share/uv/python/cpython-3.12-macos-x86_64-none/bin/python3.12 -m unittest tests.canon.test_visual_authority_rebaseline.VisualAuthorityRebaselineTests.test_binary_labels_and_failure_evidence_never_authorize tests.canon.test_visual_authority_rebaseline.VisualAuthorityRebaselineTests.test_candidate_shell_accessibility_and_proof_contract_is_exact
exit 0
Ran 2 tests; OK.

/Users/devan/.local/share/uv/python/cpython-3.12-macos-x86_64-none/bin/python3.12 -m unittest tests.canon.test_visual_authority_rebaseline
exit 0
Ran 10 tests in 20.936 seconds; OK.
```

The frozen covering command is recorded in `RECEIPT.json`. It is the one compatibility pass for the repaired candidate; the later full canon regression and governance gates remain intentionally reserved until exact-range independent review.

```text
/Users/devan/.local/share/uv/python/cpython-3.12-macos-x86_64-none/bin/python3.12 -m unittest tests.canon.test_visual_authority_rebaseline tests.canon.test_external_authority tests.canon.test_task_pack tests.canon.test_benchmark
exit 0
Ran 126 tests in 910.400 seconds; OK.
Every emitted task-pack result remained `authority_state=shadow` and stated `SHADOW task pack cannot authorize implementation`.
```

The exact result and remaining verification posture are recorded in `RECEIPT.json`.

Deterministic generation was refreshed with Python 3.12. `scripts/ambitions-canon.py build` exited `0` with `GREEN ambitions canon generated outputs`; the already-generated projections required no byte change for this proof-contract repair.

## Review posture

The implementation commit requires owner workshop and explicit approval before any authority decision, plus one exact-range independent review with both specification-compliance and code-quality verdicts, followed by the final canon regression and governance gates. Gate B remains pending. This report records no semantic approval, no task-pack authority, no legacy supersession or metadata approval, no PR/merge approval, and no cutover approval. It does not make or recommend an automatic Green transition.

## Claim ceiling

Current allowed claim: Visual design authority candidate only for the exact Figma/canon mapping scope.

This report does not claim source UI implementation, Runtime Green, rendered-app Visual Green, Accessibility Green, device readiness, privacy/legal approval, TestFlight readiness, App Store readiness, or Release Green.
