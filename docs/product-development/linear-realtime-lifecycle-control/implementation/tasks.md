# Implementation Tasks

1. Add the approved lifecycle package under `docs/product-development/linear-realtime-lifecycle-control/`. Dependency: none. Trace: REQ-001–REQ-018. Acceptance: lifecycle checker passes with no draft markers and complete Research/Scope/Design/Plan/Tasks/Verification. Proof: structural check and semantic review. Rollback/stop: remove the new isolated folder if review finds a product hard fork. Tests: lifecycle checker and `git diff --check`.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
2. Add `tools/linear-control/package.json`, TypeScript/Wrangler configuration, generated binding types, and core manifest/parser/hash models under `tools/linear-control/src/core/`. Dependency: Task 1. Trace: REQ-001, REQ-002, REQ-014. Acceptance: all complete lifecycle folders compile deterministically; raw document hashes match independent SHA-256 calculations; incomplete folders are admission-pending rather than installable. Proof: parser/hash fixtures. Rollback/stop: package remains unused if deterministic compilation fails. Tests: `npm test` and `npm run typecheck`.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
3. Add DAG scheduling, WIP, lifecycle transition, proof-gate, cycle, health, view, template, and taxonomy policies under `tools/linear-control/src/core/`. Dependency: Task 2. Trace: REQ-005–REQ-013, REQ-016. Acceptance: stable topological order, two-Project groups, one active task per Project, shared-path serialization, proof-gated Done, exact health thresholds, and controlled definitions pass exhaustive fixtures. Proof: policy tests. Rollback/stop: no mutations while any policy fixture fails. Tests: `npm test`.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
4. Add `tools/linear-control/src/adapters/linear.ts`, GitHub evidence adapter, reconciliation planner, autonomous deletion guard, and CLI commands. Dependency: Tasks 2–3. Trace: REQ-003, REQ-004, REQ-014, REQ-015, REQ-018. Acceptance: check/plan/apply/explain are deterministic; pagination/rate limiting are bounded; deletion cannot plan without complete evidence; exact mirrors verify after write. Proof: mocked GraphQL and golden-plan tests. Rollback/stop: operate read-only if a mutation or deletion contract is ambiguous. Tests: adapter and planner integration tests.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
5. Add D1 migration and `tools/linear-control/src/worker.ts` with signed Linear/GitHub/reconcile ingress, Queue consumer, DLQ-compatible retry behavior, scheduled check, health endpoint, structured logs, and kill switch. Dependency: Task 4. Trace: REQ-003, REQ-004, REQ-016–REQ-018. Acceptance: duplicate/out-of-order deliveries are idempotent, bounded ingress returns promptly, retries preserve identity, and receipts recover partial runs. Proof: Worker/D1/Queue tests. Rollback/stop: do not deploy if signature, retry, or migration tests fail. Tests: `npm test`, Wrangler types, typecheck, and dry-run deploy.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
6. Update `.github/workflows/code-quality.yml` and add the controller deployment/event workflow. Dependency: Task 5. Trace: REQ-002, REQ-007, REQ-017, REQ-018. Acceptance: controller checks run only for relevant changes; Code Quality remains the sole required branch check; signed main/check events contain no secret or document body. Proof: actionlint and workflow review. Rollback/stop: remove deployment/event jobs if secret or permission scope is broader than required. Tests: actionlint and local package checks.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
7. Provision dedicated Cloudflare D1, queue, DLQ, Worker bindings, secrets, migrations, schedules, and deploy the Worker. Dependency: Tasks 5–6. Trace: REQ-003, REQ-004, REQ-016–REQ-018. Acceptance: authenticated health, signed ingress, queue processing, D1 receipts, retry/DLQ path, structured logs, and mutation kill switch work in production. Proof: resource inventory, deployment version, health and synthetic delivery receipts. Rollback/stop: set kill switch false and roll back Worker on any production divergence. Tests: migration list/apply, deploy dry run, live health and signed synthetic event.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
8. Install or repair `Lifecycle — Linear Real-Time Lifecycle Control`, its Initiative ownership, M0–M6, five exact mirrors, labels, and one Issue per Task 1–10. Dependency: Task 7. Trace: REQ-001, REQ-008, REQ-012–REQ-014. Acceptance: Project and mirrors map one-to-one to this folder and every issue preserves dependency/proof/validation/rollback fields. Proof: post-write re-read and hash audit. Rollback/stop: keep controller read-only if exact mirror verification fails. Tests: CLI `check` and direct Linear re-read.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
9. Reconcile the entire workspace: dependencies, global ranks/groups, states, phases, milestones, cycles, health/progress, labels, views, templates, status updates, and stale objects. Dependency: Task 8. Trace: REQ-004–REQ-016. Acceptance: active/next groups are cycle-aligned, WIP is bounded, proof gates are correct, legacy usages are absent, and every planned mutation verifies. Proof: mutation receipts and portfolio index. Rollback/stop: stop only the unsafe mutation class while continuing independent repairs. Tests: two full checks against the same authority commit.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
10. Enable signed Linear webhooks, scheduled reconciliation, autonomous writes/deletions, metrics, DLQ replay, and final monitoring. Dependency: Task 9. Trace: REQ-003–REQ-018. Acceptance: two consecutive full checks converge, a synthetic drift is repaired once, a failed delivery is replayable, and no active work is orphaned. Proof: live event/run/receipt/metric evidence. Rollback/stop: disable mutations while retaining diagnostics if freshness, signature, or convergence SLOs fail. Tests: all commands in `implementation/verification.md`.
   Frontend: none — this task is limited to canon, domain, data, runtime, tooling, integration, or verification foundations and does not create or modify a user interface.
