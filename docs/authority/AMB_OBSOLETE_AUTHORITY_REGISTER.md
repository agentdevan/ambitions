# AMB_OBSOLETE_AUTHORITY_REGISTER

## Obsolete top-level references

- `Profile` as top-level root tab
- `Habits` as top-level root tab
- `Insights` as top-level root tab
- `Plan` as active top-level destination
- `Productivity task-list` style hierarchy as primary navigation
- `AI explanation` / `AI confidence` in user-facing copy

## Migration guidance

- Map obsolete references to compatibility notes where required for source archaeology.
- Do not delete historical proof; classify as historical or archive-candidate.
- New prompts must explicitly mark references as historical when surfaced.
