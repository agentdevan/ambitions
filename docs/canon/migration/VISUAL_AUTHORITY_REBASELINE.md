# Ambitions Visual Authority Rebaseline

Status: frozen candidate pending owner workshop, explicit approval, and independent review
Authority state: shadow and non-authoritative
Selected visual language: C4 — Quiet Field
Gate B: pending; task-pack selection is blocked and no visual authority transition is approved

## Result

The additive Phase 3/4 Figma corpus is represented as one deterministic, requirement-linked candidate registry. It does not change product law and it cannot authorize source work until Gate B is explicitly Green.

- Canon revision: `1`
- Canon source SHA: `ffd462ab52c0eff798071333388a051d9f3e55f3`
- Canon content SHA: `6e836710d8ed26bae3f01f5207438ebba67831b0f90cc7bded7a6368ecb51f67`
- Frozen R1 repository base: `201f1e1451a295b53fcfcefb6ba4c0acce207d45`
- New Figma file: `Oik7612LSTUHWsNRFoTlTJ`
- Retained additive pages: `17:2` through `17:11`
- R1 candidate page: `215:2` (`CANDIDATE — AV1 · Revision 1`)
- Live reconciliation root: `307:57`
- Live replacement index: `215:2`
- Product-anatomy root: `330:1667`
- Accessibility-class root: `296:35`
- Product-only proof root: `306:2`
- Exact authority nodes: `147`
- Conditionally selectable candidate masters after a future approved Gate B: `18`
- Candidate screen mappings: `47`
- Generic skeleton screens retained as failure evidence: `47`
- Product-only proof bundles: `18` (`54` hash-bound artifacts)
- Visual requirements mapped: `336/336`
- State mappings: `433/433`
- Authority-eligible states after Gate B: `411`
- Future-gated states: `22`
- Gap-blocked states: `0`
- Canonical object records: `18`
- Principal journeys: `12`
- Cross-cutting records: `11`
- Sensitive exposure channels: `9`

The normative machine record is `visual-authority-rebaseline.json`. The companion `visual-authority-r1-node-snapshot.json` binds the `49` remapped live nodes, all `18` task-pack candidate targets, the root/drilldown shell contract, and exact render digests without volatile timestamps. It also records `77` created nodes, `92` mutated/read-bound nodes, and zero deleted or destructive actions. Every tracked node carries exactly one `CANDIDATE`, `ARCHIVE`, or `FAILURE_EVIDENCE` prefix. The `18` compact candidate masters are only proposed for conditional selection after owner workshop, explicit approval, independent review, and a future Green Gate B; they have zero current task-pack authority. The `47` generic skeleton screens remain provenance and can never authorize source work.

The accessibility-size Today repair at `270:1430` has exactly one visible Search node (`359:243`) and one visible Capture node (`359:248`). The replaced hidden pairs and the visible pairs have matching geometry and fills, so the unchanged product render SHA `311374645649f6bdd851b9e34783599847c94b76d6862d0dbb2d67a473260376` is expected. This is a non-destructive, pixel-equivalent replacement receipt—not a claim that no node replacement occurred.

The exact presentation matrix limits persistent root navigation to Today, Goals, Time, and You. Capture and Search are global overlays without root chrome, and Trust/Proof is contextual inspection only. Every candidate master records the seven required variants: Standard, Large Text, Accessibility Size, Reduce Motion, Reduce Transparency, Increase Contrast, and VoiceOver Order.

## Task-pack law

The compiler validates the tracked rebaseline offline against the current UX blueprint, requirement dispositions, state inventory, screenshot bytes, and retained legacy receipt.

- While Gate B is pending, visual task packs receive a UI-readiness stop and no old or new Figma target can authorize source changes.
- Only after owner workshop, explicit approval, clean independent review, and an explicit future Green Gate B may task packs select the `18` compact `CANDIDATE` masters in `Oik7612LSTUHWsNRFoTlTJ` that resolve the declared screen, state, or requirement scope.
- `ARCHIVE` and `FAILURE_EVIDENCE` nodes are structurally ineligible even if their screen or requirement provenance overlaps a task.
- A stale canon revision/content hash, future-gated state, gap-blocked state, absent mapping, corrupted screenshot, or changed legacy receipt fails closed.
- `SWtHm9ouHTPbEFfNrrtZwv` targets are superseded in repo references and cannot be selected. They remain evidence only.

Task-pack envelopes remain ignored local artifacts. This visual registry does not replace the separate Task 24 authorization boundary.

## Figma mutation posture

The new file/page family is additive. Existing legacy Figma visual bytes were not deleted, archived, consolidated, or overwritten. The former generic screen matrix remains in the additive file as explicitly labeled failure evidence. The compact product-anatomy, accessibility-class, component, live-index, and product-only proof roots were added without changing product IA or behavior. The twelve anatomy boards are review evidence only and are excluded from product proof; each candidate master instead has a distinct hero, viewport, and presentation artifact.

The candidate registry proposes the following non-destructive legacy disposition. It is not approved, applied to Figma metadata, or authoritative while Gate B is pending:

- `15` inventoried nodes are retained as provenance, exploration, failure evidence, implementation-history evidence, privacy-boundary evidence, or unique-content sources.
- `9` formerly approved legacy targets have proposed repo-reference downgrades on this unmerged candidate branch; the proposal does not approve legacy supersession or mutate legacy Figma metadata.
- `0` destructive actions are authorized.
- Proposed future destruction remains withheld for Gate C.

The prior Task 23 receipt at `docs/canon/migration/figma-reconciliation.json` remains hash-bound provenance. Its prior approved-target semantics do not reactivate those legacy targets.

## Gate B requirements

Gate B can become Green only after the owner completes the visual workshop and gives explicit approval, the exact independent semantic and visual reviews are clean, all `18` product-only proof bundles remain hash-valid, the presentation and accessibility matrices remain exact, rollback is proven, this manifest is updated to `green`, and task-pack selection changes to `new_authority_only`. No semantic approval, metadata approval, legacy supersession, PR/merge approval, or cutover approval is inferred from this candidate. The validator rejects a partial or contradictory Green state.

This record does not itself claim that Gate B has passed.

## Rollback

- Repository rollback begins from `201f1e1451a295b53fcfcefb6ba4c0acce207d45` and reverts this bounded branch commit.
- Figma rollback removes only the additive candidate page family if separately authorized; legacy nodes remain unchanged.
- No Gate C cleanup is part of this change.

## Claim ceiling

Allowed only after owner workshop, explicit approval, required independent review, and an explicit Green Gate B receipt:

> Visual design authority Green only for the exact approved Figma/canon mapping scope.

Current candidate claim:

> Visual design authority candidate only for the exact Figma/canon mapping scope.

Forbidden:

- Source UI implemented;
- Runtime Green;
- rendered-app Visual Green;
- Accessibility Green;
- Device ready;
- Privacy/legal approved;
- TestFlight ready;
- App Store ready;
- Release Green.
