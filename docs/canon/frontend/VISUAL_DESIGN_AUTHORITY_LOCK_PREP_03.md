# Visual Design Authority Lock Prep 03

Status: Active pre-lock execution brief

Batch: `VISUAL-DESIGN-AUTHORITY-LOCK-PREP-03`

Runner command:

```bash
scripts/ambitions-codex-train.sh VISUAL-DESIGN-AUTHORITY-LOCK-PREP-03 prompts/batches/VISUAL-DESIGN-AUTHORITY-LOCK-PREP-03.md
```

## Purpose

This document is the lightweight pointer for the FAANG-level Visual + Design Authority lock-prep batch. The full execution prompt lives in:

- `prompts/batches/VISUAL-DESIGN-AUTHORITY-LOCK-PREP-03.md`

This brief does not supersede active truth files. It identifies the next consolidation move required before the Visual Encyclopedia and Design System 15 foundation can be treated as user-lockable intended final visual canon.

## Why This Exists

The repo now has two major authority systems:

1. Visual Encyclopedia / Visual 100 control plane.
2. Design System 15 token, contract, traceability, and validator foundation.

Both are valuable. They must not remain parallel authority systems. Lock Prep 03 merges them into one visual/design authority model with mature-surface coverage, provenance, token-to-recipe mapping, dashboard consistency, and zero-residue quality gates.

## Required Outcome

The batch must produce one authoritative visual/design status, not competing dashboard interpretations.

The expected final authority system must prove or explicitly expose:

- every mature App Store surface expected in final Ambitions
- every current recipe's origin and source relationship
- every recipe's planned implementation batch or missing-batch gap
- every token's recipe/surface/primitive/contract relationship
- every missing recipe, missing source, missing batch, and missing implementation target
- zero exact duplicate residue
- zero near-duplicate residue
- no generic or templated active authority prose
- no false implementation, release, device, or accessibility proof claim

## Non-Goals

This batch must not implement production SwiftUI UI, generate screenshots, activate hosted CI, or claim the app is visually finished.

Implementation remains intentionally deferred until the user marks the Visual Encyclopedia / design authority as locked.

## Model Path

Use:

1. GPT-5.5 planning
2. GPT-5.4-mini bounded patch
3. GPT-5.5 review / repair / final readiness

Do not use Spark terminology in this batch or successor reports.

## Authority Boundary

This file is an execution pointer. The detailed instructions are in the batch prompt. Active product truth still begins with:

- `docs/truth/**`
- `docs/canon/frontend/**`
- `DesignTokens/**`, if validated as token source truth

Generated reports are proof of validation run state only. They are not implementation proof.
