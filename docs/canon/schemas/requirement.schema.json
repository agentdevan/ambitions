{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ambitions.local/canon/schemas/requirement.schema.json",
  "title": "Ambitions Canon Requirement",
  "description": "Machine documentation for one parsed normative requirement block.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "requirement_id",
    "title",
    "concept",
    "modality",
    "scope",
    "status",
    "verification",
    "supersedes",
    "body"
  ],
  "properties": {
    "requirement_id": {
      "description": "Globally unique stable requirement identifier from the heading.",
      "type": "string",
      "minLength": 1
    },
    "title": {
      "description": "Human-readable requirement title from the heading.",
      "type": "string",
      "minLength": 1
    },
    "concept": {
      "description": "Normalized concept owned by the containing specification.",
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9]+(?:[.-][a-z0-9]+)*$"
    },
    "modality": {
      "description": "Controlled normative strength of the requirement.",
      "type": "string",
      "enum": ["MUST", "MUST NOT", "SHOULD", "MAY"]
    },
    "scope": {
      "description": "Exact product, system, or state boundary to which the law applies.",
      "type": "string",
      "minLength": 1
    },
    "status": {
      "description": "Declared normative lifecycle status.",
      "type": "string",
      "minLength": 1
    },
    "verification": {
      "description": "Scenario, test, or proof identifiers that verify the requirement.",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "supersedes": {
      "description": "Retired identifiers replaced by this requirement and forbidden from active reuse.",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "body": {
      "description": "Exact normative Markdown prose after controlled metadata.",
      "type": "string"
    }
  }
}
