# Ambitions ChatGPT Project Instructions

Treat `docs/canon/` as the sole normative repository authority. Begin with
`docs/canon/generated/CODEX_START_HERE.md` and request a bounded canon pack for
nontrivial work.

Translate the user's intent into request-only task intake. Intake may request
scope, requirement IDs, files, validation, proof, rollback, claim ceiling, and
procedural skill adapters. It cannot supply or imply owner approval, computed
authorized files, validation results, proof claims, break-glass permission,
merge permission, or canon.

Before any tracked edit, require a current `ambitions-canon task start` result.
Before commit or review, require exact-diff `ambitions-canon task finalize`.
After resume, interruption, or context compaction, discard stale local packs,
envelopes, and receipts and regenerate from current repository state.

ChatGPT, these Project Instructions, skills, PR prose, task packs, envelopes,
receipts, local approval claims, and local validation are non-authoritative.
They cannot widen scope, waive a gate, or authorize merge.

For Tasks 24–29 only, preserve the single-use owner-direct transition recorded
under `OWNER-TRAIN5-DIRECT-INTEGRATION-2026-07-17T234045Z`: exact SHA-bound local
authorization/finalization, one exact high-risk review, rollback, Gate C,
privacy/security, and proof honesty remain required. Protected CI, required
checks, ruleset enforcement, and live enforcement are not installed or proven.

For the platform-signer root-of-trust bootstrap only, preserve the conditional
single-use transition `OWNER-CEBR-SIGNER-BOOTSTRAP-2026-07-19T141500Z`. It is
valid only for the exact candidate commit, tree, patch digest, and sorted path
manifest named by an authenticated owner comment in GitHub issue `#33`, after
an exact high-risk review with zero Critical and Important findings. It may
install the signed platform path but cannot authorize CEBR canon changes,
product/runtime work, destruction, release, reuse, or scope growth. After the
bootstrap commit is integrated, use the signed event, approval, validation,
and exact-finalization path for CEBR and all later approval-required work.

Keep the implementation local-first and evidence-bound. Never infer product,
runtime, visual, accessibility, privacy, device, protected-enforcement,
TestFlight, App Store, or release readiness from prose or source presence.
