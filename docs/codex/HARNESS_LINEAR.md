# Harness Linear

Linear tracks bounded Slice 1 work. The repo remains the source of truth.

Each issue should include goal, scope, runner batch, acceptance gates, manual handoff, artifact paths, rollback, and non-claims.
