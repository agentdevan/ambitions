# LifeShape Pressure Jargon Failure

Status: Red fixture

This fixture preserves the failure where root Time exposes invented instrumentation language instead of human reality copy.

Must not recur:

- `Compression ridge` in root UI
- `open layer` or `pressure layer` as primary copy
- semanticMeaning terrain labels carrying root meaning
- pressure state shown as abstract dashboard text
- root copy that requires learning Ambitions internal metaphors

Required replacement: plain user-facing Time language such as `Afternoon gets tight after Team sync`, `Needs buffer after 5:00 PM`, or `One light window is open now`.
