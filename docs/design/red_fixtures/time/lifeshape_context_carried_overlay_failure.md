# LifeShape Context Carried Overlay Failure

Status: Red fixture

This fixture preserves the failure where root Time exposes routing/source-context language inside the LifeShape surface.

Must not recur:

- `Context carried` on root Time
- `source context preserved` on root Time
- linked-route/debug/navigation receipt language in the primary surface
- overlays pasted over the LifeShape object without user intent
- trust/source/proof/routing copy shown as ambient root UI

Required replacement: remove the overlay entirely, or use a tiny transient shell receipt outside the primary object only when user context requires it.
